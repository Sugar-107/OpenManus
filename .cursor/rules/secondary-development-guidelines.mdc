---
description: 
globs: 
alwaysApply: true
---
# OpenManus二次开发指南

## 关键能力提升维护
你每次遇到问题修复后，必须将刚才的问题维护到[项目核心知识管理.md]中，依次来提高你代码的准确率

## 项目概览
OpenManus是一个开源的通用AI代理框架，允许创建具有多种工具能力的智能代理。它支持浏览器自动化、Python代码执行、文件操作、命令行交互和MCP(Model Context Protocol)连接等功能。

项目结构基于一个灵活的基于工具的代理架构，核心包括：
- 基础代理类结构：BaseAgent -> ReActAgent -> ToolCallAgent -> 其他特化代理
- 工具系统：BaseTool及各种派生工具
- LLM连接模块：支持主流大语言模型API
- Prompt系统：各种角色的系统提示词和上下文提示词

## 开发思路

### 1. 扩展新工具
如需添加新工具，需要实现以下步骤：

1. 在`app/tool/`目录下创建新的工具类文件
2. 继承`BaseTool`类并实现所需接口
3. 定义工具名称、描述和参数结构
4. 实现`execute`方法处理工具逻辑
5. 在相应代理类的`available_tools`中注册新工具

示例代码结构：
```python
from app.tool.base import BaseTool

class MyNewTool(BaseTool):
    name: str = "my_new_tool"
    description: str = "对工具的详细描述"
    parameters: dict = {
        "type": "object",
        "properties": {
            "param1": {
                "type": "string",
                "description": "参数1的说明",
            },
            # 其他参数...
        },
        "required": ["param1"],
    }

    async def execute(self, param1: str, **kwargs) -> str:
        # 实现工具逻辑
        return f"工具执行结果: {param1}"
```

### 2. 创建自定义代理
要创建自定义代理，可参考以下步骤：

1. 在`app/agent/`目录下创建新的代理类文件
2. 继承合适的基类（如`ToolCallAgent`或`BrowserAgent`）
3. 定义代理名称、描述和设置
4. 自定义`system_prompt`和`next_step_prompt`
5. 配置`available_tools`和其他必要属性
6. 根据需要重写`think`、`act`或其他方法

示例代码结构：
```python
from pydantic import Field
from app.agent.toolcall import ToolCallAgent
from app.prompt.custom import SYSTEM_PROMPT, NEXT_STEP_PROMPT
from app.tool import ToolCollection, MyNewTool, Terminate

class MyCustomAgent(ToolCallAgent):
    name: str = "my_custom_agent"
    description: str = "自定义代理描述"

    system_prompt: str = SYSTEM_PROMPT
    next_step_prompt: str = NEXT_STEP_PROMPT

    max_steps: int = 20

    available_tools: ToolCollection = Field(
        default_factory=lambda: ToolCollection(
            MyNewTool(), Terminate()
        )
    )

    async def think(self) -> bool:
        # 自定义思考逻辑
        return await super().think()
```

### 3. 优化提示词
可在`app/prompt/`目录下创建或修改提示词文件，包括：

1. 系统提示词：定义代理角色和行为规范
2. 上下文提示词：引导代理下一步行动
3. 特殊情况处理提示词：如错误处理、多媒体响应等

提示词设计关键点：
- 清晰定义代理角色和能力范围
- 明确任务处理流程和决策标准
- 提供足够上下文和工具使用示例
- 设置适当的约束和安全指导

### 4. 添加新的运行方式
要添加新的运行方式，可参考以下步骤：

1. 创建新的运行脚本（如`run_custom.py`）
2. 参考现有的`main.py`、`run_mcp.py`或`run_flow.py`
3. 实例化自定义代理并添加必要的初始化
4. 实现命令行参数解析和交互逻辑

### 5. 扩展LLM支持
如需支持更多LLM，修改`app/llm.py`并添加相应配置支持。

## 高级自定义

### 1. 多代理协作
参考`run_flow.py`实现多代理协作系统，可结合工具链设计和消息传递机制。

### 2. 自定义工具链
设计特定任务的工具组合，通过ToolCollection灵活组织。

### 3. 外部集成
通过实现新工具，将外部服务和API集成到代理系统中。

## 提示词优化建议

1. 任务分解提示：引导代理将复杂任务分解为子任务
2. 工具选择提示：帮助代理选择最适合当前步骤的工具
3. 反思与评估提示：促使代理评估执行结果并调整策略
4. 安全与限制提示：确保代理行为符合安全准则
5. 记忆管理提示：帮助代理有效管理长期任务的上下文

## 示例场景
可基于本框架构建以下应用场景：

1. 智能网页助手：专注于网页浏览和内容提取
2. 开发辅助代理：结合代码执行和文件编辑能力
3. 数据分析代理：专注于数据处理和可视化
4. 自动化测试代理：用于软件测试和质量保证

代理继承结构参考
BaseAgent (基础抽象类)
  |
  +--> ReActAgent (ReAct范式基础实现)
        |
        +--> ToolCallAgent (工具调用代理)
              |
              +--> BrowserAgent (浏览器代理)
              |     |
              |     +--> Manus (通用代理)
              |
              +--> MCPAgent (MCP协议代理)


核心工具能力参考
1. python_execute - 执行Python代码
2. browser_use_tool - 浏览器自动化工具
   - 网页导航（go_to_url, go_back, refresh等）
   - 元素交互（click_element, input_text等）
   - 内容提取（extract_content）
   - 标签管理（switch_tab, open_tab, close_tab）
3. str_replace_editor - 文件编辑工具
   - 查看文件（view）
   - 创建文件（create）
   - 替换内容（str_replace）
   - 插入内容（insert）
   - 撤销编辑（undo_edit）
4. 终止工具（Terminate）- 结束代理执行
5. 其他工具如终端、网页搜索等

OpenManus/
├── app/
│   ├── agent/           # 代理类实现
│   ├── tool/            # 工具类实现
│   ├── prompt/          # 提示词系统
│   └── llm.py           # LLM连接模块
├── config/              # 配置文件
├── main.py              # 主运行脚本
├── run_mcp.py           # MCP版本运行脚本
└── run_flow.py          # 多智能体版本运行脚本
